export const metadata = {
  title: "Tenant Credit Analysis Tool — Free Screening",
  description: "Screen tenant credit the way underwriters do: ratings, rent-to-revenue, lease term, guarantees — and how tenant quality moves cap rates and value.",
};

export default function Layout({ children }) {
  return children;
}
