export const metadata = {
  title: "Lease Type Analyzer — NNN vs Gross vs Modified",
  description: "Sort any commercial lease into its true type — gross, single/double/triple net, or modified gross — and see what the rent number really means for NOI.",
};

export default function Layout({ children }) {
  return children;
}
