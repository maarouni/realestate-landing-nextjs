export const metadata = {
  title: "IRR Calculator for Real Estate — Free & Instant",
  description: "Free real estate IRR calculator: total return over your hold period, equity multiple, and total profit — with 2026 IRR benchmarks by investment strategy.",
};

export default function Layout({ children }) {
  return children;
}
